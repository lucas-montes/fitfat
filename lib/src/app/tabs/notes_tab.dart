import 'package:flutter/material.dart';

import '../../notes/screens/notes_screen.dart';

final class NotesTab extends StatelessWidget {
  const NotesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const NotesScreen();
  }
}
