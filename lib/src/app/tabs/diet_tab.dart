import 'package:flutter/material.dart';

import '../../diet/screens/meal_list.dart';

final class DietTab extends StatelessWidget {
  const DietTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const MealListScreen();
  }
}
