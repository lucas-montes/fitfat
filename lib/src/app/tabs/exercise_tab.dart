import 'package:flutter/material.dart';

import '../../exercise/screens/workout_list.dart';

final class ExerciseTab extends StatelessWidget {
  const ExerciseTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const WorkoutListScreen();
  }
}
